---
driver:
  name: docker

provisioner:
  name: chef_zero

verifier:
  name: inspec

platforms:
  - name: ubuntu-14.04

suites:
  - name: default
    run_list:
      - recipe[opsworks_cookbook_demo::default]
    verifier:
      inspec_tests:
        - test/recipes
    attributes:
